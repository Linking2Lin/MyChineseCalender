# 2026-09-06 审查问题修复记录

对应 `CODE_REVIEW_2026-09-06.md` 的 15 项问题。本轮在原工作区修改，未提交、未发布、未在设备安装应用。

**布局处理已更正：** 用户要求保留原有精调外观，已撤回 R12 的布局改写及新增日期前缀，恢复 `09ec2be` 的尺寸分支、胶囊边距、头像比例、字号、两行文字和 Provider 参数。保留日期订阅、缓存隔离、刷新与图片保存逻辑；空态只补充点击动作。后续逻辑修复与注释维护不改变组件外观。

恢复后验证：Small/Medium/Max 布局函数体、视觉常量、Provider XML 和原有预览与 `09ec2be` 一致；Debug/Release 完整构建成功，现有 29 个 JVM 测试全部通过。未在设备安装或核验桌面截图。

| 问题 | 修复结果 | 验证 |
| --- | --- | --- |
| R01 Release 编译失败 | Glance 预览迁至 `Appwidget/src/debug`，主源码不再引用预览依赖 | Debug/Release 构建 |
| R02 活跃 Glance 会话不刷新 | 组合持续观察按日仓库及头像版本；数据库初始读取和后续状态更新进入同一数据流 | 仓库观察者先空后有数据、二次刷新回归测试；设备显示待验收 |
| R03 跨天同步 | 动态日期广播、时间/时区/开机/更新广播、午夜非精确闹钟、无网络约束的周期兜底、次日预取 | 日期变化、回拨、DST 与午夜计算测试；OS 调度仍可能延迟 |
| R04 旧数据冒充当天 | 移除跨日期 fallback；状态保留查询日期；失败时仍请求更新展示；按用户要求不添加日期前缀 | 昨日/未来缓存隔离测试；宿主延迟刷新仍受系统调度限制 |
| R05 页面恢复不刷新 | STARTED 生命周期重新检查日期；前台日期流跨天更新，旧请求取消并隔离结果；增加手动刷新和状态提示 | ViewModel 切日、前台再次刷新及失败保留当天缓存测试 |
| R06 空态无法重试 | 点击回调置于共同外层容器，空态/加载/失败/成功均可触发；刷新锁阻止重复操作 | 代码检查，实际空态点击待设备验收 |
| R07 数据库单例竞争 | 锁内再次检查实例 | 代码检查 |
| R08 数据库异常逃逸 | 统一仓库捕获缓存读写故障，保留可用网络结果和错误状态；取消异常继续传播 | 缓存异常、恢复持久化、ViewModel 故障展示测试 |
| R09 严格解析导致扩字段失败 | 共用 `NetworkJson`，显式拒绝非成功 HTTP 状态 | 模型解析及 Ktor MockEngine 测试 |
| R10 残缺响应覆盖缓存 | 必须包含有效匹配的公历与非空农历；拒绝错误日期/无效日期/残缺内容 | 校验与缓存保留测试 |
| R11 更新错误仍显示成功 | 逐实例继续更新并汇总失败，刷新结果区分数据、持久化和更新请求失败 | 单实例失败继续更新、取消传播测试 |
| R12 布局边界 | 撤回本项视觉修改，按用户要求完整保留原有布局，不施加新的字号、头像或缩放限制 | 与 `09ec2be` 原始布局及 Provider 比对；保留原有预览 |
| R13 裁剪位置 | 居中正方形源区域，再绘制圆形蒙版 | 横图/竖图几何测试 |
| R14 图片保存竞争 | 编码与写入互斥、独立临时文件、选择序号保护最终提交；失败保留旧文件 | 旧编码未完成时二次选择、编码失败及临时文件清理测试 |
| R15 缓存无限追加 | Room v4 按日期主键 upsert，按日期查询和清理；2→3→4 迁移 | 唯一缓存行为测试、schema 导出、升级仪器测试包编译；真实迁移未在设备执行 |

同时处理了重复 HKO 获取入口、小组件删除后仍持续同步、取消异常处理、无用的大型图标依赖、页面日期/诗词错误状态、工程说明与 `.kotlin/` 忽略规则。原有 Small/Medium/Max 布局保留。Release 开启压缩和资源裁剪，并显式保留用于持久化/回调的 Glance 和 Worker 类名。

新增 GitHub Actions 编译与回归检查。仪器测试包含 v2/v3 升级、当天缓存去重、保留最后有效记录、过滤坏数据及保留 HKO 表的断言；它已完成 APK 编译，不表示已经在真实 Android SQLite/Room 上运行通过。

验证命令：

```sh
./gradlew :app:assembleDebug :app:assembleRelease testDebugUnitTest lintDebug :Module_Base:assembleDebugAndroidTest --continue --console=plain
```

以下记录为恢复布局前的验证：组合构建成功，新增 27 个 JVM 业务回归测试，连同原有 3 个模板测试共 30 个，失败、错误、跳过均为 0。恢复布局时移除了只约束新布局的 1 个测试，现有测试数为 29；下方包体数据为恢复前历史记录，不代表恢复后的 APK 大小。

| 检查 | 最终结果 |
| --- | --- |
| Debug | 构建成功；APK 48,751,235 字节，约 46.49 MiB |
| Release | R8 压缩、资源裁剪与打包成功；未签名 APK 3,530,094 字节，约 3.37 MiB |
| JVM 测试 | app 4 个、Appwidget 12 个、Module_Base 14 个；Module_Poem 无测试源码 |
| Lint | 四个模块均无 Error；Warning 分别为 app 34、Appwidget 2、Module_Base 1、Module_Poem 0，主要是依赖维护、未使用资源和代码风格提示，未禁用检查 |
| 数据库升级仪器测试 | 测试 APK 编译成功；没有配置可运行的模拟器，本轮未在 Android 设备执行 |
| 压缩产物检查 | Glance、刷新回调、同步 Worker 的类名与入口构造器保留；未引用的 `img_maodie.jpg` 未进入 Release APK |
| 差异检查 | `git diff --check` 通过 |

移除大型扩展图标依赖后，Debug 从审查时约 77 MiB 降到 46.49 MiB。本轮代码在启用 R8 前的 Release 为 34.82 MiB，启用后为 3.37 MiB；Debug 与 Release 用途不同，不把二者直接对比作为优化收益。构建首次因离线缓存缺少 `compose-group-mapping` 失败，补齐依赖后上述整组检查通过。

优化项 O01–O06 的处理覆盖统一仓库、按小组件生命周期调度、取消传播、业务回归与 CI、依赖精简和发布压缩，以及错误提示与 README；布局精简已撤回。未使用图片保留在源码中，由资源裁剪从发布包排除。

保留的边界：没有批量升级第三方依赖；没有运行线上 API 契约测试或目标 One UI 桌面实测。后台采用非精确调度，省电限制或强行停止会造成延迟，不声称绝对零点刷新。较早使用不同数据库文件名的历史版本是否需要额外兼容，仍以实际分发范围为准。
